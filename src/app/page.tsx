import { Hero } from "./hero.tsx";

/**
 * La home de la aplicación (ticket #65): reemplaza el placeholder
 * "Hello world!" por el hero — eyebrow, título, divisor, bajada y la barra
 * de estadísticas del catálogo.
 */
export default function Home() {
  return (
    <main>
      <Hero />
    </main>
  );
}
